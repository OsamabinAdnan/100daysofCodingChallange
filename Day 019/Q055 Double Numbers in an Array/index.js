"use strict";
// Starts with a list of numbers
let num = [1, 2, 3, 4, 5];
// Doubles each number
let doubleNumber = num.map((num) => num * 2);
// Shows the new list of doubled numbers
console.log(doubleNumber);
//Show the original list of numbers
console.log(num);
