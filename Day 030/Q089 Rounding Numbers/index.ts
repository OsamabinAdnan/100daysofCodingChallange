// This function rounds a number to two decimal places
function roundingNumber (num:number){
    return num.toFixed(2)  // Rounds and converts back to number
}

console.log(roundingNumber(3.14159));
// π is now rounded off, making it simpler to work with.